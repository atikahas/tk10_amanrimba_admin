import './bootstrap';

import Alpine from 'alpinejs';
import focus from '@alpinejs/focus';
window.Alpine = Alpine;

Alpine.plugin(focus);

Alpine.start();

// import { Datatable, initTE, Datepicker, Input, Collapse, Dropdown, Modal, Ripple,} from "tw-elements";
// initTE({ Datatable, Datepicker, Input, Collapse, Dropdown, Modal, Ripple  });